# modular-dockers
Docker templates to accelerate deployment


To run the docker compose:

```shell
docker-compose up
```

or

```shell
docker-compose up -d
```


To stop the container:

```shell
docker-compose down
```
