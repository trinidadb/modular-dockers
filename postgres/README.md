# postgres-docker

To run the docker compose:

```shell
docker-compose up
```

or, if you want to detach the shell,

```shell
docker-compose up -d
```


To stop the container:

```shell
docker-compose down
```
